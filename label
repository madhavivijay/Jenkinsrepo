pipeline {
  agent none
  stages {
    stage ('build') { 
      ageny { label 'cslave' }
      steps {
      sh '''
      #!/bin/bash
      pwd
      echo "this is a build stage"
     '''
   }
  }
    stage ('deploy') {
      steps {
      agent { label 'master' }
     echo "this is a deploy stage"
   }
  }
 }
}
