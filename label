pipeline {
  agent ( label 'cslave' )
  stages {
    stage ('build') { 
      steps {
      sh '''
      #!/bin/bash
      pwd
      echo "this is a build stage"
     '''
   }
  }
    stage ('deploy') {
      steps {
     sh '
     echo "this is a deploy stage"
    '
   }
  }
 }
}
