pipeline {
  agent { label 'cslave' }
  stages {
    stage ('build') { 
      steps {
      sh '''
      #!/bin/bash
      pwd
      echo "this is a build stage"
     '''
   }
  }
    stage ('deploy') {
      steps {
     echo "this is a deploy stage"
   }
  }
 }
}
